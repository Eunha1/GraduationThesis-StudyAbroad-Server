export class newCommission {
  customer_phone: string;
  tuition: string;
  percentage: string;
  note: string;
  expected_time: Date;
  time_of_receipt: Date;
  amount: string;
}

export class pagination {
  page: number;
  limit: number;
}
